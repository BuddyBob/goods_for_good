import './MarketPopup.css';

import React, { useRef, useState } from 'react';

const MarketPopup = ({onClose,itemId, item}) => {

  
  

  return (
    <div>
      <input type="button" className="btn btn-primary claim-1" value="Claim"/>
    </div>
  );
}

export default MarketPopup;
